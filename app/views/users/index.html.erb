<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
	<% @users.each do |user| %>
    	<li>
	      <%= gavatar_for user %>
    	  <%= link_to user.name, user %>
	    </li>
	<% end %>
</ul>
